%h1 New violation

= simple_form_for [@violation.labor, @violation] do |f|
  = f.error_notification

  .form-inputs
    = f.input :description
    = f.input :amount_due

  .form-actions
    = f.button :submit

= link_to 'Back', labor_path(@violation.labor)
