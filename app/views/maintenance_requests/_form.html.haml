= simple_form_for(@maintenance_request) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :user
    = f.input :description
    = f.input :done

  .form-actions
    = f.button :submit
